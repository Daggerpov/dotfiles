# Built neovim runtime/install — not dotfiles (nvim *config* lives in .config/nvim/)
neovim/

# OS / editor noise
.DS_Store
*.swp

# Machine-local KiRoom dev-desktop host (written by ~/bin/kiroom-remote.sh; not for the public repo)
.config/kiroom/
